" vim: filetype=vim foldmethod=marker fileformat=unix

" name and site
let g:this_meta.name = expand('<sfile>:p:t')
let g:this_meta.site = 'https://github.com/JazzCore/ctrlp-cmatcher.git'

" extra settings for neobundle
let g:this_meta.neodict.name = g:this_meta.name
let g:this_meta.neodict.build = { 
      \ 'unix'    : './install_linux.sh',
      \ 'windows' : 'install_windows.bat'
      \ }

function g:this_meta.config()
  " TODO: your plugin configuration goes here.
  " currently can not compile ctrlp_cmather on windows.
  if has('unix')
    let g:ctrlp_match_func = {'match' : 'matcher#cmatch' }
  endif
endfunction
