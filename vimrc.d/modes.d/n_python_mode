" vim: filetype=vim foldmethod=marker fileformat=unix

" Set Tile & Bundle Manager {{{1

call SetTitle('python mode')

call SetBundleManager('NeoBundle')

" }}}1

" Merge Configs {{{1

let s:sub_configs = [
	\ 'x_essential',
	\ 'x_common',
	\ 'x_ext_tools',
	\ 'x_color',
	\ 'x_textobj',
	\ 'x_python',
	\ ]

call MergeConfigs(s:sub_configs)

" }}}1

" Bundles in Current Config {{{1

let s:common_bundle = [
      \ ]

if has('unix')
  call add(s:common_bundle, 'ycm')
  call add(s:common_bundle, 'autoformat')
endif

let s:bundle_list = s:common_bundle

call AddBundles(s:bundle_list)
" }}}1