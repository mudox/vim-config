" vim: filetype=vim foldmethod=marker fileformat=unix

" Set Tile & Bundle Manager {{{1

call SetTitle('title for the mode') " TODO: give you mode a title

call SetBundleManager('NeoBundle') " TODO: specify you preferred bndle manager.

" }}}1

" Merge Configs {{{1

" TODO: put every sub-mode names you need into the list.
"let s:sub_configs = [
      "\ 'x_essential'
      "\ ]

call MergeConfigs(s:sub_configs)

" }}}1

" Bundles in Current Config {{{1

" TODO: put every meta nemas you need into the list.
let s:bundle_list = [
      \ 'rt_go',
      \ 'rt_perl',
      \ 'rt_json',
      \ 'rt_markdown',
      \ 'rt_ruby'
      \ 'rt_swift',
      \ 'rt_toml',
      \ ]

call AddBundles(s:bundle_list)
" }}}1
