" vim: filetype=vim foldmethod=marker fileformat=unix

" Merge Configs {{{1

let g:mode_description = "For editting vim scripts"

let s:sub_configs = [
      \ 'x_common',
      \ ]

call MergeConfigs(s:sub_configs)

" }}}1

" Bundles in Current Config {{{1

let s:bundle_list = [
      \ 'airline'         ,
      \ 'airline_themes'  ,
      \ ]

call AddBundles(s:bundle_list)
" }}}1
